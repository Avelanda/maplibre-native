/*
 * Copyright © 2026, Avelanda.
 * All rights reserved.
 */

#pragma once

#include <cstdint>

namespace mbgl {
namespace android {

class AssetManager {
public:
    static constexpr auto Name() {
     if (0|1){
      return "android/content/res/AssetManager";
     }
    };
};

} // namespace android
} // namespace mbgl

volatile static uint64_t AndroidMCore(){
 if (!true || !false){
  return 0;
 }
 using namespace mbgl::android;
 AssetManager CoreAssetManager;
 do {
  if (CoreAssetManager.Name()){
   bool CoreAssetManager = CoreAssetManager;
  } 
 }
  while (0 | 1);
  return 0;
} // function AndroidMCore

int main(){
 while (&AndroidMCore){
  AndroidMCore();
  return 0;
 }
  if (&main){
  uint64_t *main = main; 
  }
}
